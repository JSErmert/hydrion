# CLAUDE HYDRION → UI BINDING PROMPT

You are integrating HydrOS Machine View with real Hydrion state.

This is NOT a redesign task.

---

## PRIMARY GOAL

Replace mock state with **real simulation data** while preserving:

- layout
- labels
- structure
- machine identity

---

## ARCHITECTURE

You must separate:

1. Hydrion truth state
2. Adapter layer
3. UI components

---

## REQUIRED TYPES

```ts
type HydrionTruthState = { ... };
type HydrosDisplayState = { ... };
REQUIRED MAPPING

Implement:

mapHydrionTruthToDisplayState(truth)

This function must:

be explicit
be readable
avoid hidden logic
BINDING RULES
Flow
from q_processed_lmin
display in L/min
Pressure
from dp_total_pa
convert to kPa
Fouling
from fouling_frac_s1/s2/s3
convert to %
Backflush
from bf_active / pulse index
Bypass
from bypass_active
STATE MODES

Support:

"mock"
"sim"
ANIMATION RULES

Smooth:

flow
pressure

Do NOT smooth:

backflush pulses
bypass activation
CONSTRAINTS

Do NOT:

redesign UI
invent metrics
change labels
DELIVERABLES
Refactored React code
Types
Mapping function
Integration explanation
FINAL STANDARD

If the UI looks real but isn't state-driven → FAIL
If it is state-driven but unreadable → FAIL

Bind reality into HydrOS.