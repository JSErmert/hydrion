# M8.2 — Execution Document Prompt

First, write this prompt content to:
`docs/orchestration/M8_Foundation/M8.2_execution_document_prompt.md`

If the folder path does not yet exist, create it first.

Then execute the task exactly as written below.

Operate under HydrOS x AlignFlow Orchestration Contract v3.1 at:
`docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing prerequisite artifact, or ambiguity that would risk milestone integrity.

## Objective

Create the authoritative execution document for:

**M8 — Deployment Gap Quantification / Privileged-Channel Ablation**

Write the output to:
`docs/orchestration/M8_Foundation/M8.3_execution_document.md`

Also create one required insight file for this pass at:
`docs/orchestration/insights/M8_Foundation/M8.2_insight_<short_slug>.md`

If the insights folder path does not yet exist, create it first.

After creating each markdown artifact, also print the full markdown in chat per Contract v3.1.

## Governing inputs

Use the following as binding inputs:

- `docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`
- `docs/orchestration/M8_Foundation/M8.1_refinement_output.md`
- `docs/orchestration/M8_Foundation/M8.1R.2_research_brief.md`
- `docs/orchestration/M8_Foundation/M8.1R.4_sources_map.md`
- `docs/orchestration/insights/M8_Foundation/M8.0_insight_actuator_channels_misclassified_as_privileged.md`
- `docs/orchestration/insights/M8_Foundation/M8.1R.1_insight_<actual_existing_slug>.md`
- `docs/orchestration/insights/M8_Foundation/M8.1R.3_insight_source_map_defines_m8_claim_validity_boundary.md`

Use actual repo code state as source of truth where file paths, schemas, scripts, and artifact names matter.

## Context

M8's chosen direction is now fixed:

- quantify the deployment gap by removing the 6 genuinely non-deployment physics-truth channels
- preserve actuator-feedback channels `6–9`
- preserve sensor-derived channels `10–13`
- train `ppo_hydrienv_v2` on `obs8_deployment_v1`
- compare against:
  - `ppo_hydrienv_v1`
  - random baseline

The research and source-traceability layer already established:

### Non-negotiable source-grounded M8 constraints
1. new VecNormalize fit required for `obs8_deployment_v1`
2. matched training conditions required between v1 and v2
3. calibration-pending sensor noise must be named as an explicit confound
4. the comparison must be framed as a cross-schema deployment-gap measurement, not policy improvement

### Literature-thin / non-recoverable M8 limitations
1. no direct flow-control RL ablation precedent in the HydrOS domain
2. no direct proof that actuator history is sufficient for fouling-phase inference
3. no literature support for ablation validity under placeholder noise
4. no literature support for single-seed sufficiency

These constraints and limitations must be reflected directly in M8.3.

## Purpose

M8.3 must translate the M8 research layer into an implementation-ready execution document that defines:

- exact schema scope
- exact training target
- exact evaluation design
- exact benchmark boundaries
- exact acceptance criteria
- exact prohibited claims
- exact interpretation rules for outcome scenarios

This is not yet the implementation plan and not the implementation execution prompt.

## Required contents of M8.3

### 1. Executive objective
State the exact M8 question in one concise paragraph.

### 2. Inherited system state
Summarize what M8 inherits from M7 and the M8 research layer, including:
- `ppo_hydrienv_v1`
- `obs14_v1`
- the 3-class channel taxonomy:
  - `0–5` physics_truth
  - `6–9` actuator_feedback
  - `10–13` sensor_derived
- preserved CCE lineage

### 3. Formal schema definition
Define `obs8_deployment_v1` explicitly.

Include:
- exact retained indices
- exact removed indices
- semantic meaning of each retained group
- normalization continuity or required changes
- any naming/version rules

Be explicit that `obs8_deployment_v1` is deployment-realistic, not sensor-only.

### 4. Policy artifact objective
Define the new canonical M8 artifact objective:
- `ppo_hydrienv_v2`
- associated VecNormalize stats
- associated metadata JSON
- role of the random baseline

State that M8 does not replace `ppo_hydrienv_v1`; it complements it as a deployment-gap comparison artifact.

### 5. Scope boundaries
Define what M8 is in scope to do and what it is not.

At minimum, state that M8 does **not**:
- change CCE
- change M7 artifacts
- change reward design
- do calibration work
- prove deployment readiness
- prove sensor-only sufficiency
- establish generalization or robustness

### 6. Mandatory methodology constraints
Create a section explicitly labeled something like:

**Methodology Validity Conditions**

This section must include the four source-grounded constraints, with their research/source-map basis:
1. VecNorm refit required
2. matched training conditions required
3. calibration confound must be named
4. cross-schema deployment-gap framing required

These must be treated as non-negotiable execution conditions.

### 7. Training and evaluation design
Define the high-level execution target for:
- training `ppo_hydrienv_v2`
- evaluating:
  - `ppo_hydrienv_v2`
  - `ppo_hydrienv_v1`
  - random baseline

State that comparison validity depends on matched conditions where appropriate.

### 8. Acceptance criteria
Create concrete M8 acceptance criteria.

They should include, at minimum:
- `obs8_deployment_v1` exists and is correctly defined
- training script path is correct
- eval path compares v2, v1, and random correctly
- new VecNorm stats are fit to obs8
- v1 VecNorm stats are not reused for v2
- metadata clearly identifies cross-schema comparison context
- sensor/actuator channels are active in evaluation
- M7 artifacts remain untouched
- CCE artifacts remain untouched
- interpretation guardrails are explicitly carried into the result

### 9. Four-scenario interpretation table
Include the result-interpretation table derived from the sources map.

It should distinguish how to interpret:
- v2 ≈ v1
- v2 < v1 with small gap
- v2 < v1 with large gap
- v2 > random

And it must also state what **must not** be concluded from each outcome.

This table should live inside or directly adjacent to the AC interpretation section so it cannot be lost later.

### 10. Prohibited claims
Create an explicit section listing claims M8 must not make.

At minimum:
- "deployment-ready"
- "channels 0–5 are unnecessary"
- "sensor-only control is proven"
- "deployment is impossible"
- "v2 is better than v1" as a generic claim

### 11. Risks and confounds
Summarize the four literature-thin areas and any additional practical M8 execution risks.
These should be framed as acknowledged limitations, not hidden caveats.

### 12. Next artifact
State what should follow M8.3.
This should likely be:
`docs/orchestration/M8_Foundation/M8.4_implementation_plan.md`

## Critical instructions

- Do not drift into implementation details that belong in the implementation plan
- Do not weaken the research-grounded constraints
- Do not let internal design choices sound literature-mandated
- Do not let the execution document sound more ambitious than M8 really is
- Do not blur deployment-realistic with deployment-validated
- Keep benchmark lineage absolutely clean

## Required return

Return:
1. `docs/orchestration/M8_Foundation/M8.2_execution_document_prompt.md`
2. `docs/orchestration/M8_Foundation/M8.3_execution_document.md`
3. the insight file path created under `docs/orchestration/insights/M8_Foundation/`
4. short summary of the exact M8 execution objective
5. the next recommended artifact after M8.3
