# M7.5 — Implementation Execution Prompt

First, write this prompt content to:
`docs/orchestration/M7_Foundation/M7.5_implementation_execution_prompt.md`

Then execute the task exactly as written below.

Operate under the HydrOS Co-Orchestrator Execution Contract.

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing file, or ambiguity that would risk benchmark lineage, schema integrity, or implementation correctness.

## Objective

Execute the approved M7.4 implementation plan for the first canonical HydrionEnv baseline under `obs14_v1`.

## Governing document

Use this implementation plan as binding:
- `docs/orchestration/M7_Foundation/M7.4_implementation_plan.md`

## Required sequencing

Follow this sequence exactly:

1. Store this prompt as:
   - `docs/orchestration/M7_Foundation/M7.5_implementation_execution_prompt.md`
2. Execute the M7.4 implementation plan exactly as written
3. Write the execution result to:
   - `docs/orchestration/M7_Foundation/M7.6_execution_report.md`
4. Return:
   - M7.5 path
   - M7.6 path
   - concise implementation summary
   - test/training/eval result summary
   - whether M7 is ready for formal closeout

## Scope

Implement only the approved M7 baseline path:

- create `hydrion/train_ppo_hydrienv_v1.py`
- create `hydrion/eval_ppo_hydrienv_v1.py`
- train `ppo_hydrienv_v1` under HydrionEnv / `obs14_v1`
- produce:
  - `models/ppo_hydrienv_v1.zip`
  - `models/ppo_hydrienv_v1_vecnorm.pkl`
  - `models/ppo_hydrienv_v1_meta.json`
- run evaluation
- verify AC1–AC14 from M7.4
- keep CCE and `ppo_cce_v2` untouched

## Constraints

- Do not modify CCE files
- Do not modify stale HydrionEnv scripts
- Do not redesign reward
- Do not broaden into robustness / curriculum / deployment work
- Do not alter `obs14_v1`
- Do not relax benchmark-lineage discipline
- If any step fails, report the exact failure rather than silently working around it

## Required execution actions

1. Create `hydrion/train_ppo_hydrienv_v1.py` exactly per M7.4 Section 2.1
2. Create `hydrion/eval_ppo_hydrienv_v1.py` exactly per M7.4 Section 2.2
3. Verify imports
4. Run training
   - full run if feasible from current repo/runtime state
   - if a shorter proof run is necessary first, state that explicitly and then continue as justified by M7.4
5. Verify artifact creation
6. Run evaluation
7. Run required test suite / validation commands
8. Check AC1–AC14 explicitly
9. Write M7.6 execution report with full technical truth

## Required contents of M7.6

1. Final status
2. Files created / changed
3. Training outcome
4. Artifact outcome
5. Evaluation outcome
6. Sensor diagnostic outcome
7. AC1–AC14 checklist with pass/fail
8. Confirmation that CCE and `ppo_cce_v2` were untouched
9. Commit hash if committed
10. Whether M7 is ready for closeout

## Critical instructions

- Prefer exact implementation truth over optimistic phrasing
- Do not claim deployment readiness
- Do not claim CCE/HydrionEnv comparability
- Do not say the baseline proves sensor-only control sufficiency
- If training did not fully complete, say so clearly and report the true partial state

## Return

After storing M7.5 and generating M7.6, return:

1. `M7.5_implementation_execution_prompt.md` path
2. `M7.6_execution_report.md` path
3. concise implementation summary
4. whether M7 is ready for formal closeout
