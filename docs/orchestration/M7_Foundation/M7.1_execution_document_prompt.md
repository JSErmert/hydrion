# M7.1 — Execution Document Prompt

First, write this prompt content to:
`docs/orchestration/M7_Foundation/M7.1_execution_document_prompt.md`

Then execute the task exactly as written below.

Operate under the HydrOS Co-Orchestrator Execution Contract.

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing file, or ambiguity that would risk milestone-planning integrity.

## Objective

Create the authoritative M7 baseline RL execution document and store it as the next numbered deliverable.

Write the output to:
`docs/orchestration/M7_Foundation/M7.2_baseline_RL_execution_document.md`

## Required sequencing

Follow this sequence exactly:

1. Store this prompt as:
   - `docs/orchestration/M7_Foundation/M7.1_execution_document_prompt.md`
2. Then generate and write the execution document as:
   - `docs/orchestration/M7_Foundation/M7.2_baseline_RL_execution_document.md`
3. Then return:
   - the stored M7.1 prompt path
   - the generated M7.2 execution document path
   - a concise summary of the M7 baseline RL direction
   - whether M7.2 is ready for planning activation

Do not skip the storage step.
Do not leave the execution document only in chat output.

## Governing foundation documents

Use the following as binding inputs:

- `docs/orchestration/M7_Foundation/M7.1R.1_research_brief.md`
- `docs/orchestration/M7_Foundation/M7.1R.2_sources_map.md`

Use current repo reality as the source of truth where code/document state matters.

## Inherited technical state

- M5 is closed.
- M6.1 is closed: core sensor realism implemented.
- M6.2 is closed: no safe slot substitutions existed within `obs12_v2`.
- M6.2B is closed: `obs14_v1` is active for HydrionEnv only.
- HydrionEnv now exposes:
  - indices `0–11` unchanged from prior semantics
  - index `12` = `flow_sensor_lmin / 20.0`
  - index `13` = `dp_sensor_kPa / 80.0`
- CCE remains unchanged and benchmark-protected.
- `ppo_cce_v2` remains the canonical CCE / truth-derived benchmark artifact.
- No canonical HydrionEnv policy artifact yet exists for `obs14_v1`.

## Purpose

This execution document must define the scope, constraints, artifact expectations, benchmark lineage, and acceptance criteria for the **first canonical HydrionEnv baseline policy trained under `obs14_v1`**.

This is a baseline-establishment phase only.

It is **not**:
- reward redesign
- robustness expansion
- curriculum learning
- deployment validation
- CCE migration
- benchmark replacement for CCE

## Scope

### In scope
- defining the first canonical HydrionEnv RL baseline objective under `obs14_v1`
- selecting the correct HydrionEnv training/evaluation path
- identifying scripts, artifact outputs, and metadata that must be produced
- defining valid vs invalid benchmark comparisons
- defining minimum validation and acceptance criteria for the first HydrionEnv `obs14_v1` artifact

### Out of scope
- reward redesign
- robustness/generalization expansion
- curriculum training
- CCE observation changes
- CCE benchmark replacement
- hardware calibration
- deployment claims
- schema redesign beyond the already accepted `obs14_v1` reality

## Requirements

1. Treat this as the first canonical HydrionEnv baseline under `obs14_v1`.
2. Preserve benchmark lineage clarity:
   - `ppo_cce_v2` remains canonical for CCE
   - HydrionEnv under `obs14_v1` requires a new canonical artifact
3. Do not imply comparability between CCE benchmark numbers and the new HydrionEnv baseline unless explicitly justified.
4. Keep M7 baseline scope narrow:
   - establish the training/evaluation path
   - define canonical artifact(s)
   - define validation
5. Preserve simulation-only claim discipline.
6. Explicitly state what the M7 baseline will and will not prove.
7. Distinguish clearly between:
   - source-grounded guidance from the research brief
   - internal architecture decisions
8. Keep all claims about partial observability, delay handling, privileged information, sim-to-real, and comparability consistent with the M7.1R research layer.

## Required contents of M7.2

### 1. Executive objective
One concise paragraph defining the M7 baseline rebuild goal.

### 2. Inherited system state
Summarize what M7 inherits from:
- M5
- M6.1
- M6.2
- M6.2B

State clearly:
- which schema is active where
- which benchmark artifact remains canonical where

### 3. Canonical baseline artifact objective
Define the first required HydrionEnv baseline artifact under `obs14_v1`.

Include:
- expected artifact naming
- expected companion metadata file(s)
- required schema label
- seed discipline
- reproducibility expectations

### 4. Environment and schema scope
Define clearly:
- HydrionEnv only
- `obs14_v1` only
- CCE excluded and unchanged
- no CCE benchmark replacement in this phase

### 5. Research-informed baseline design assumptions
Use the research brief and sources map to state the baseline assumptions for:
- mixed truth + sensor observation regime
- 1-step sensor delay handling
- fixed placeholder noise vs domain randomization
- privileged information limitation
- artifact comparability rules

For each, distinguish:
- source-derived rationale
- internal architecture decision

### 6. Training scope boundaries
State:
- what prior RL infrastructure should be reused
- what existing scripts are stale or unsuitable if applicable
- what must remain untouched
- what comparisons are valid
- what comparisons are invalid

### 7. Validation and evaluation expectations
Define minimum required outputs for the first baseline, including:
- training artifact(s)
- evaluation artifact(s)
- metadata
- diagnostic metrics
- any required replay / smoke / integrity checks

### 8. Acceptance criteria
Provide a concrete list of completion conditions for the first canonical HydrionEnv `obs14_v1` baseline.

### 9. Blocked-by / blocking statements
Include:
- Blocked by: M6.2B — satisfied
- Blocking: later reward shaping / robustness work until the baseline artifact is established

### 10. Risks and failure conditions
Address at minimum:
- benchmark lineage confusion
- schema confusion
- comparing CCE and HydrionEnv improperly
- treating the first baseline as deployment-valid
- confusing privileged-training success with deployment realism

## Critical instructions

- Be technically conservative.
- Do not overclaim.
- Do not let CCE and HydrionEnv benchmark histories blur together.
- Do not assume "more RL" is the goal; the goal is a clean canonical baseline.
- Do not silently convert internal architecture decisions into literature-backed claims.
- Prefer the narrowest honest baseline path over a broader, more ambitious one.

## Return

After storing M7.1 and generating M7.2, return:

1. `M7.1_execution_document_prompt.md` path
2. `M7.2_baseline_RL_execution_document.md` path
3. a concise summary of the M7 baseline RL direction
4. whether M7.2 is ready for planning activation
