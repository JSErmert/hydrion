# M7.3 — Planning Refinement Prompt

First, write this prompt content to:
`docs/orchestration/M7_Foundation/M7.3_planning_refinement_prompt.md`

Then execute the task exactly as written below.

Operate under the HydrOS Co-Orchestrator Execution Contract.

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing file, or ambiguity that would risk implementation-planning integrity.

## Objective

Refine the M7.2 baseline RL execution document into a tighter implementation-ready plan so that only the intentionally accepted, documented limitations remain.

Write the output to:
`docs/orchestration/M7_Foundation/M7.4_implementation_plan.md`

## Purpose

This is not a new milestone and not a broader redesign.

The goal is to convert M7.2 into a precise implementation plan with:
- no avoidable ambiguity
- no naming drift
- no benchmark-lineage confusion
- no stale-script confusion
- no hidden comparability mistakes
- no weak metadata discipline

Accepted baseline limitations may remain, but they must be explicit and controlled:
- truth channels 0–9 remain privileged training inputs
- sensor noise remains calibration-pending
- no domain randomization yet
- no deployment validity
- no CCE migration

## Governing inputs

Use as binding inputs:
- `docs/orchestration/M7_Foundation/M7.1R.1_research_brief.md`
- `docs/orchestration/M7_Foundation/M7.1R.2_sources_map.md`
- `docs/orchestration/M7_Foundation/M7.2_baseline_RL_execution_document.md`

Use current repo reality as the source of truth where code paths, script status, or artifact state matter.

## Resolved clarification (pre-decided before generation)

Stale HydrionEnv scripts (`train_ppo.py`, `train_ppo_v15.py`, `eval_ppo.py`) are left
physically untouched. They are explicitly excluded by name from the M7 execution path.
No deletions. No deprecation annotations. No modifications to those files unless a later
milestone explicitly scopes that work.

## Required refinement targets

### 1. Artifact naming lock
Make the implementation plan fully consistent on:
- `ppo_hydrienv_v1`
- `ppo_hydrienv_v1_vecnorm.pkl`
- `ppo_hydrienv_v1_meta.json`

Do not allow alternate spellings, legacy names, or stale references.

### 2. Script-path clarity
Resolve exactly:
- which old HydrionEnv scripts are stale
- whether they should be ignored, deprecated in docs, or left untouched but explicitly excluded
- which new scripts must be created for M7 baseline and their exact paths

### 3. Schema/version lock
Require explicit implementation checks for:
- `obs14_v1`
- observation space shape `(14,)`
- metadata schema field consistency
- no accidental fallback to old HydrionEnv obs paths

### 4. Benchmark-lineage guard
Refine the plan so it explicitly prevents:
- invalid CCE vs HydrionEnv benchmark comparison
- invalid obs12 vs obs14 artifact comparison
- reuse of incompatible VecNormalize statistics
- any report language that makes these paths sound directly comparable

### 5. Privileged-information guard
Make the implementation plan explicitly preserve the distinction between:
- baseline establishment
- deployment realism

The plan must clearly state that:
- truth channels remain privileged
- M7 success does not prove sensor-only control sufficiency
- no deployment claim is allowed from this phase

### 6. Sensor-channel diagnostics
Tighten the plan's requirements for proving that obs14_v1 is actually active and meaningful:
- obs[12] and obs[13] must be observed in evaluation
- they must be non-zero and varying
- they must be demonstrably distinct from legacy channels when appropriate
- diagnostics must be saved or printed in a reproducible way

### 7. Metadata strictness
Refine the metadata requirements so the implementation plan explicitly includes:
- schema version
- env
- seed
- timesteps
- reward version
- privileged truth channel warning
- calibration-pending noise warning
- deployment_ready = false
- note field making artifact lineage limitations machine-readable

### 8. Validation and acceptance criteria tightening
Turn the M7.2 execution document into an implementation plan with:
- file-by-file implementation order
- exact validation steps
- exact pass/fail criteria
- exact definition of what constitutes a valid first baseline artifact

### 9. Risks and failure conditions
Refine the risk section so the implementation plan only carries:
- real risks that affect implementation correctness or interpretation integrity
- explicit mitigations for each
- no vague risks without operational consequence

## Required output structure

The implementation plan must contain:

1. Executive implementation strategy
2. File-by-file implementation plan
3. Script creation/modification list
4. Artifact naming and metadata lock
5. Schema/version lock checks
6. Training and evaluation sequence
7. Diagnostics and validation plan
8. Benchmark-lineage guardrails
9. Accepted limitations that remain
10. Risks, mitigations, and failure conditions
11. Acceptance criteria
12. Ready-for-execution verdict

## Critical instructions

- Do not broaden scope into reward redesign, robustness, curriculum, or deployment.
- Do not change CCE.
- Do not relax baseline claim discipline.
- Do not invent new research claims beyond the M7.1R foundation.
- Prefer narrower and cleaner implementation guidance over broader and more ambitious plans.
- The goal is an implementation-ready baseline plan, not a research essay.

## Return

After storing M7.3 and generating M7.4, return:

1. `M7.3_planning_refinement_prompt.md` path
2. `M7.4_implementation_plan.md` path
3. a concise summary of how the plan was tightened
4. whether M7.4 is now ready for execution
