# M7.0 — Baseline RL Rebuild Execution-Document Prompt

First, write this prompt content to:
`docs/orchestration/M7_Foundation/M7.0_claude_execution_document_prompt.md`

Then execute the task exactly as written below.

Operate under the HydrOS Co-Orchestrator Execution Contract.

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing file, or ambiguity that would risk milestone-planning integrity.

## Objective

Create the authoritative M7 baseline RL execution document.

Write the output to:
`docs/orchestration/M7_Foundation/M7.1_baseline_RL_rebuild_execution_document.md`

## Context

- M5 is closed.
- M6.1 is closed: core sensor realism implemented.
- M6.2 is closed: observation handoff analysis found no safe slot substitutions within `obs12_v2`.
- M6.2B is closed: `obs14_v1` is now active for HydrionEnv only.
- HydrionEnv now exposes:
  - indices `0–11` unchanged from prior semantics
  - index `12` = `flow_sensor_lmin / 20.0`
  - index `13` = `dp_sensor_kPa / 80.0`
- CCE remains unchanged and benchmark-protected.
- `ppo_cce_v2` remains the canonical CCE / truth-derived benchmark artifact.
- No canonical HydrionEnv policy artifact yet exists for `obs14_v1`.

## Purpose

This execution document should define the scope, constraints, and acceptance criteria for the first canonical HydrionEnv baseline policy trained under `obs14_v1`.

## Scope

### In scope
- defining the M7 baseline rebuild objective
- selecting the correct environment path for the first HydrionEnv `obs14_v1` baseline
- identifying training/evaluation artifacts that must be produced
- defining benchmark lineage and comparison rules
- defining validation and acceptance criteria for the first HydrionEnv `obs14_v1` artifact

### Out of scope
- reward redesign
- robustness/generalization expansion
- curriculum training
- CCE observation changes
- CCE benchmark replacement
- hardware calibration
- deployment claims

## Requirements

1. Treat this as the first canonical HydrionEnv baseline under `obs14_v1`.
2. Preserve benchmark lineage clarity:
   - `ppo_cce_v2` remains canonical for CCE
   - HydrionEnv `obs14_v1` requires a new canonical artifact
3. Do not imply comparability between CCE benchmark numbers and the new HydrionEnv baseline unless explicitly justified.
4. Keep M7 baseline scope narrow:
   - establish train/eval path
   - define canonical artifact(s)
   - define validation
5. Preserve simulation-only claim discipline.
6. Explicitly state what M7 baseline will and will not prove.

## Required contents of the execution document

1. **Executive objective**
   - one-paragraph definition of the M7 baseline rebuild goal

2. **Current inherited system state**
   - what M7 inherits from M5, M6.1, M6.2, and M6.2B
   - which observation schema is now active where

3. **Baseline artifact objective**
   - define the first required canonical artifact for HydrionEnv under `obs14_v1`
   - artifact naming expectations
   - metadata expectations

4. **Environment and schema scope**
   - HydrionEnv path only, unless a broader path is strongly justified
   - `obs14_v1` as the active HydrionEnv schema
   - CCE unchanged and explicitly excluded from this phase unless stated otherwise

5. **Training scope boundaries**
   - what should be reused from earlier RL infrastructure
   - what should remain untouched
   - what comparisons are valid vs invalid

6. **Validation and evaluation expectations**
   - minimum benchmark outputs required
   - what diagnostics must be recorded
   - what counts as a valid first baseline

7. **Acceptance criteria**
   - define what must be true before M7 baseline can be considered complete

8. **Blocked-by / blocking statements**
   - Blocked by: M6.2B — satisfied
   - Blocking: later reward shaping / robustness work until the baseline is established

9. **Risks and failure conditions**
   - benchmark lineage confusion
   - schema confusion
   - comparing CCE and HydrionEnv improperly
   - treating the first baseline as deployment-valid

## Critical instructions

- Be technically conservative.
- Do not assume that "more RL" is the goal; the goal is a clean canonical baseline.
- Do not overclaim.
- Do not broaden into reward shaping or robustness.
- Do not let CCE and HydrionEnv benchmark histories blur together.

## Return

Return:
1. document path
2. short summary of the M7 baseline direction
3. whether the execution document is ready for planning activation
