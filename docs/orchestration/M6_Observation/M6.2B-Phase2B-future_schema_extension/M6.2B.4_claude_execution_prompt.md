# M6.2B.4 — Claude Execution Prompt

Operate under the HydrOS Co-Orchestrator Execution Contract.

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing file, or ambiguity that would risk schema integrity, benchmark lineage, or observation semantics.

## Objective

Execute the approved M6.2B implementation plan for the HydrionEnv-only add-only schema extension to `obs14_v1`.

## Governing document

Use this implementation plan as binding:
- `docs/orchestration/M6_Foundation/M6.2B-Phase2B-future_schema_extension/M6.2B.3_implementation_plan.md`

## Scope

Implement only the approved minimum-safe schema extension:

- HydrionEnv observation path only
- `obs14_v1`
- indices `0–11` unchanged
- index `12` = `sensors["flow_sensor_lmin"]` normalized by `20.0 L/min`
- index `13` = `sensors["dp_sensor_kPa"]` normalized by `80.0 kPa`
- CCE unchanged and protected
- `ppo_cce_v2` remains unaffected

## Constraints

- Do not modify the semantic meaning of indices `0–11`
- Do not substitute sensor values into existing slots
- Do not modify CCE observation construction
- Do not touch reward logic
- Do not begin M7 work
- Preserve `truth_state` / `sensor_state` separation
- Preserve benchmark lineage clarity
- If any implementation detail threatens semantic clarity, choose the narrower safer path

## Required implementation actions

1. Modify `hydrion/sensors/sensor_fusion.py`
   - append the two new observation dimensions
   - update docstring/version label to `obs14_v1`
   - include an explicit semantic warning and index table if specified by the implementation plan

2. Modify `hydrion/env.py`
   - update observation space shape from `(12,)` to `(14,)` for the HydrionEnv path only
   - fix any stale schema comments so they match `obs14_v1`

3. Create and run `tests/test_obs14_v1.py`
   - cover shape
   - source traceability
   - bounds
   - truth-state contamination
   - semantic preservation of indices `0–11`
   - explicit CCE guard

4. Run the relevant test suite and confirm all prior tests still pass alongside the new tests

5. Commit only if the implementation passes validation cleanly

## Required output

Return:
1. files changed
2. exact implementation summary
3. test results
4. confirmation that CCE was untouched
5. confirmation that `ppo_cce_v2` remains valid and unaffected
6. confirmation that `obs14_v1` is now active for HydrionEnv
7. whether the implementation is ready for formal closeout
8. commit hash if committed

## Do not do

- do not redesign any existing observation dimensions
- do not broaden scope into Phase 2b redesign
- do not add extra sensor-derived dimensions beyond indices 12 and 13
- do not create backward-compatibility shims
- do not change CCE to "match" HydrionEnv
- do not invalidate `ppo_cce_v2`
