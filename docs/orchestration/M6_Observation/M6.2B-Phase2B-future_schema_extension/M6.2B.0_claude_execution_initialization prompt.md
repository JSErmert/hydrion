Operate under the HydrOS Co-Orchestrator Execution Contract.

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing file, or ambiguity that would risk schema-design integrity.

Objective:
Produce the M6.2b.0 schema extension design brief for HydrOS.

Write the document to:
docs/orchestration/M6_Phase2b/M6.2b.0_schema_extension_design_brief.md

Context:
- M6 Phase 1 is complete: dp_sensor_kPa and flow_sensor_lmin exist in sensor_state.
- M6 Phase 2 analysis is complete: no safe slot substitutions exist within obs12_v2.
- obs12_v2 is preserved unchanged.
- Future sensor-derived RL inputs now require a deliberate schema extension rather than substitution.
- ppo_cce_v2 remains the canonical benchmark artifact and must be protected from accidental invalidation.

Scope:
This is a schema-design and research brief only.
Do not implement code.
Do not write an execution plan yet.
Do not redesign rewards.
Do not start Phase 3.
Do not assume a new schema version name until justified by the analysis.

Requirements:
1. Determine the minimum architecturally honest path for introducing sensor-derived RL inputs.
2. Evaluate whether the next schema should be:
   - an add-only extension (for example 12D to 14D), or
   - a broader redesign, if and only if strongly justified.
3. Evaluate whether the extension should apply to:
   - HydrionEnv first, with CCE deferred, or
   - both paths together,
   and justify the recommendation.
4. Define which new sensor-derived dimensions are candidates for inclusion.
5. Define normalization strategy candidates for each new dimension.
6. Define benchmark lineage implications:
   - what remains valid from ppo_cce_v2
   - what becomes diagnostic only
   - what new baseline artifact would eventually be required
7. Define versioning rules:
   - when a version bump is required
   - what minimum versioning strategy is appropriate
   - how to avoid silent schema drift
8. Explicitly identify:
   - what can be decided now
   - what remains assumption-based
   - what requires later hardware calibration
9. Use trusted published sources only for any research-grounded claims.
   - Prefer peer-reviewed scientific literature and authoritative technical references.
   - Do not rely on uncited intuition.
   - If a design conclusion is architectural rather than literature-derived, label it clearly as an internal architecture decision rather than an externally sourced fact.

Research focus:
- observation schema design discipline for control systems / ML pipelines where measurement channels differ from latent state
- trustworthy handling of new measured variables in versioned RL observation spaces
- any published or authoritative guidance relevant to preserving comparability across policy artifacts when input schema changes
- any trustworthy evidence relevant to measured flow / differential-pressure channels as distinct control signals rather than substitutes

Deliverable structure:
1. Executive conclusion
2. Current-state constraints
3. Candidate schema extension strategies
4. Recommended default path
5. Candidate new observation dimensions
6. Normalization strategy options
7. HydrionEnv vs CCE rollout recommendation
8. Benchmark / artifact lineage implications
9. Versioning recommendation
10. Open questions and calibration-pending items
11. Source notes / citations policy

Critical instructions:
- Preserve truth_state / sensor_state separation.
- Do not treat normalization convenience as semantic equivalence.
- Do not invalidate ppo_cce_v2 casually.
- Prefer the narrowest honest schema extension over a larger redesign.
- If literature is thin, say so explicitly.

Return:
- the document path
- a short summary of the recommended schema direction
- whether the brief is ready to serve as the basis for M6.2b.1 execution-document drafting