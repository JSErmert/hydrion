# M6.2B.2 — Claude Planning Activation Prompt

Operate under the HydrOS Co-Orchestrator Execution Contract.

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing file, or ambiguity that would risk implementation-planning integrity.

## Objective

Activate planning for M6.2B schema extension implementation using the completed design brief as the governing source.

## Source documents

Primary:
- `docs/orchestration/M6_Phase2b/M6.2B.1_schema_extension_design_brief.md`

Relevant upstream context:
- `docs/orchestration/M6_Phase2/Phase2_M6_observation_handoff.md`
- `docs/orchestration/M6_Phase2/Phase2_M6_implementation_plan.md`
- current post-M5 locked blueprint and M5→M6 transition documents as needed

## Planning task

Produce the full implementation plan for the recommended schema-extension path.

Current recommended direction from the design brief:
- `obs14_v1`
- add-only extension
- HydrionEnv first
- CCE frozen / deferred
- indices `0–11` unchanged
- index `12` = `sensors["flow_sensor_lmin"]` normalized by `20.0 L/min`
- index `13` = `sensors["dp_sensor_kPa"]` normalized by `80.0 kPa`
- `ppo_cce_v2` remains protected and unchanged
- HydrionEnv `obs12_v2` artifacts become incompatible with `obs14_v1` and require a new baseline later

## Requirements

1. Do not implement code yet.
2. Do not broaden scope into reward redesign, Phase 3 RL execution, or CCE observation migration.
3. Preserve `truth_state` / `sensor_state` separation.
4. Preserve existing semantic meaning of indices `0–11`.
5. Treat `obs14_v1` as a candidate design direction to be operationalized carefully, not casually.
6. Keep benchmark lineage explicit:
   - `ppo_cce_v2` remains canonical for CCE / truth-derived benchmark history
   - HydrionEnv `obs14_v1` will require a new baseline artifact later
7. If any implementation detail threatens semantic clarity, choose the narrower safer path.

## Required planning outputs

### 1. File-by-file implementation plan
Specify exactly which files must be:
- created
- modified
- left untouched

At minimum, assess:
- `hydrion/sensors/sensor_fusion.py`
- `hydrion/env.py`
- `hydrion/state/init.py`
- any schema/version registry or artifact metadata locations
- any docs files needed for schema traceability

### 2. Exact observation mapping plan
Document the final intended `obs14_v1` mapping:
- indices `0–11` unchanged from current meaning
- index `12` source, physical quantity, normalization, expected range
- index `13` source, physical quantity, normalization, expected range

### 3. Normalization and bounds plan
For indices `12` and `13`, confirm:
- normalization formula
- why the chosen denominator is valid
- whether clipping is required
- whether any extra normalized sensor keys should or should not be added to `sensor_state`

### 4. Schema/version plan
Define:
- exact version label to use
- where version labels/comments/docstrings must be updated
- what must be recorded so there is no silent schema drift
- how `obs12_v2` and `obs14_v1` are distinguished in docs and artifact lineage

### 5. Backward-compatibility and artifact-lineage plan
Explain clearly:
- what remains valid from `ppo_cce_v2`
- what becomes diagnostic only
- what new HydrionEnv baseline artifact will eventually be required
- what should be stated in metadata / docs to prevent miscomparison

### 6. Validation plan
Include tests for:
- observation length is now 14 where applicable
- indices `0–11` remain unchanged semantically
- index `12` populates from `sensor_state["flow_sensor_lmin"]`
- index `13` populates from `sensor_state["dp_sensor_kPa"]`
- all observation values remain bounded
- no truth-state contamination
- end-to-end env rollout sanity
- explicit distinction between HydrionEnv upgraded path and unchanged CCE path

### 7. Risks and failure conditions
Address at minimum:
- accidental modification of indices `0–11`
- accidental CCE observation mutation
- silent schema drift
- normalization misuse
- confusion between `P_in` and `dp_total`
- misleading artifact comparability

### 8. Acceptance criteria
Provide final implementation acceptance criteria for M6.2B.

## Critical instructions

- Do not redesign the old 12 indices.
- Do not substitute new sensor values into existing slots.
- Do not touch CCE unless the plan proves it is absolutely necessary, which the current brief does not recommend.
- Do not invalidate `ppo_cce_v2`.
- Prefer explicit documentation over implicit compatibility assumptions.

## Deliverable

Write the output to:
- `docs/orchestration/M6_Phase2b/M6.2B.3_implementation_plan.md`

And return:
1. the document path
2. a concise summary of the implementation strategy
3. confirmation whether the plan is ready for execution
