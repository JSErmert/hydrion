Operate under the HydrOS Co-Orchestrator Execution Contract.

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing file, or ambiguity that would risk Phase 2 implementation planning integrity.

Objective:
Activate implementation planning for M6 Phase 2 — observation handoff — using the stored execution document as the governing specification.

Source document:
docs/orchestration/M6_Phase2/Phase2_M6_observation_handoff.md

Planning task:
Produce the full implementation plan for Phase 2 before any code changes are made.

Requirements:
1. Start from the execution document and treat its constraints, field audit, and acceptance criteria as binding.
2. Do not implement code yet.
3. Do not pre-assume obs12_v3 unless the field-by-field audit proves a semantic source change.
4. Do not conflate HydrionEnv and CCE observation paths.
5. Preserve truth_state / sensor_state separation.
6. Keep benchmark integrity and ppo_cce_v2 comparability in view at all times.

Required planning outputs:

1. **Field-by-field source audit**
   - HydrionEnv / sensor_fusion.py path
   - CCE / conical_cascade_env.py path
   - for each index:
     - current source key
     - physical quantity
     - normalization
     - available sensor_state equivalent
     - semantic match vs proxy mismatch
     - migrate / defer / extend / keep-truth decision

2. **Resolution of open questions Q1–Q4**
   - answer each explicitly
   - identify evidence from current codebase
   - state any remaining uncertainty

3. **Default conservative implementation recommendation**
   - explicitly recommend the minimum safe Phase 2 scope
   - likely candidates:
     - HydrionEnv index 0: evaluate for migration only if semantic equivalence is confirmed
     - HydrionEnv index 1: default defer unless strongly justified
     - CCE path: default defer unless a clean sensor injection path is proven without benchmark corruption

4. **Schema/version assessment**
   - can obs12_v2 remain valid?
   - if not, what exact index/source change forces a version bump?
   - what is the minimum acceptable schema strategy?
   - if versioning changes are path-specific, document that explicitly

5. **File-by-file implementation plan**
   - files to modify
   - files to create
   - exact scope per file
   - what must remain untouched

6. **Validation plan**
   - divergence tests
   - range tests
   - contamination tests
   - end-to-end rollout test
   - diagnostic ppo_cce_v2 rollout delta

7. **Benchmark integrity note**
   - explicitly state that any ppo_cce_v2 rollout on a changed observation path is diagnostic only, not a valid new benchmark comparison, because ppo_cce_v2 was trained on truth-derived obs12_v2

8. **Risks and failure conditions**
   - schema dishonesty
   - semantic substitution mistakes
   - accidental CCE benchmark invalidation
   - hidden truth/sensor contamination
   - misleading backward-compat assumptions

9. **Acceptance criteria**
   - restate the final conditions required before Phase 2 implementation can begin

Important instructions:
- Normalization convenience does not erase semantic mismatch.
- Adding a normalized sensor key does not justify replacing P_in with ΔP unless the semantic change is explicitly accepted and versioned.
- Protect the canonical ppo_cce_v2 benchmark artifact from accidental invalidation.
- Prefer deferral over ambiguous migration.

Return:
- the full Phase 2 implementation plan
- schema/version recommendation
- minimum safe implementation scope
- whether the plan is ready for execution