# M9.1R.3 — Verified Source Pass (Sensor Calibration Grounding)

First, write this prompt content to:
`docs/orchestration/M9_Foundation/M9.1R.3_verified_source_prompt.md`

If the folder path does not yet exist, create it first.

Then execute the task exactly as written below.

Operate under:

- `docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`
- `docs/orchestration/governance/HydrOS_Orchestrator_Execution_Schema_v1.md`
- `CLAUDE.md`

---

## Objective

Convert the M9.1R.2 preliminary research brief into a **verified, source-grounded calibration layer** using the device classes committed in M9.2A.

This is the **first pass that produces commit-ready numeric values** for M9.

---

## Governing Inputs

- `M9.1R.2_research_brief.md` (structure only)
- `M9.2A_G1_sensor_class_resolution.md` (device class commitment — binding)
- `M9.1B.2_system_reality_adherence_audit.md` (code-level grounding)
- `M9.2_pre_execution_readiness_synthesis.md` (final scope + protections)

---

## Calibration Targets (ONLY)

### Differential Pressure
- `dp_drift_rate_kPa_per_step`
- `dp_drift_max_kPa`
- `dp_fouling_gain`

### Optical
- `turbidity_noise_std`
- `scatter_noise_std`

---

## Exclusions (STRICT)

Do NOT modify:
- `flow_noise_frac`
- `flow_bias_std_lmin`
- `dp_noise_kPa`
- `dp_latency_steps`
- ANY optical gain coefficients
- ANY physics-layer parameters

---

## Required Work

For EACH parameter:

### 1. Find real-world sources
Use:
- manufacturer datasheets
- instrumentation standards
- credible engineering references

### 2. Extract value ranges
Identify:
- typical values
- worst-case bounds
- operating assumptions

### 3. Convert to HydrOS units
Include:
- unit transformations
- time scaling (per step vs per hour/year)
- normalization explanation

### 4. Assign final calibrated value

### 5. Record provenance

---

## Output File

Write:

`docs/orchestration/M9_Foundation/M9.1R.3_verified_source_brief.md`

---

## Required Structure

### 1. Executive Summary
- Which parameters are now grounded
- Confidence levels

---

### 2. Parameter Grounding Table

| Parameter | Value | Units | Source | Confidence | Notes |

---

### 3. Per-Parameter Justification
- Device class
- Source explanation
- conversion logic

---

### 4. Assumptions
- Any extrapolation or interpretation

---

### 5. Remaining Unknowns
- What still requires experiment (optical mapping)

---

### 6. M9 Readiness Confirmation
State:
- sensor layer now lifted from L2 → L3/L4
- M9.3 can now commit values

---

## Critical Rules

- DO NOT invent sources
- DO NOT guess values
- DO NOT use training knowledge without framing
- EVERY value must have a source OR be explicitly derived
- If no source exists → mark as "not grounded" (do NOT fake it)

---

## Required Return

Return:

1. prompt path
2. output path
3. insight path
4. short summary of grounded parameters
5. confirmation that M9.3 can now proceed
