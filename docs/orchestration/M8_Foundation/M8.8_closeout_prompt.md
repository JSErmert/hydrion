# M8.8 — Closeout Prompt

First, write this prompt content to:
`docs/orchestration/M8_Foundation/M8.8_closeout_prompt.md`

If the folder path does not yet exist, create it first.

Then execute the task exactly as written below.

Operate under HydrOS x AlignFlow Orchestration Contract v3.1 at:
`docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true contradiction across governing artifacts that would risk closeout integrity.

## Objective

Create the formal closeout record for:

**M8 — Deployment Gap Quantification / Privileged-Channel Ablation**

Write the output to:
`docs/orchestration/M8_Foundation/M8.9_closeout_record.md`

Also create one required insight file for this pass at:
`docs/orchestration/insights/M8_Foundation/M8.8_insight_<short_slug>.md`

If the insights folder path does not yet exist, create it first.

After creating each markdown artifact, also print the full markdown in chat per Contract v3.1.

## Governing inputs

Use the following as binding inputs:

- `docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`
- `docs/orchestration/M8_Foundation/M8.3_execution_document.md`
- `docs/orchestration/M8_Foundation/M8.5_implementation_plan.md`
- `docs/orchestration/M8_Foundation/M8.7_execution_report.md`
- `docs/orchestration/M8_Foundation/M8.1_refinement_output.md`
- `docs/orchestration/M8_Foundation/M8.1R.2_research_brief.md`
- `docs/orchestration/M8_Foundation/M8.1R.4_sources_map.md`
- all M8 insight files currently stored in:
  `docs/orchestration/insights/M8_Foundation/`

Use actual repo state as source of truth where artifact paths and file existence matter.

## Closeout purpose

M8.9 must become the authoritative milestone record that states:

- what M8 set out to measure
- what was implemented
- what was actually observed
- what M8 does and does not prove
- what changed in HydrOS because of M8
- what the next milestone now inherits

This is a closeout artifact, not a new design document.

## Required contents of M8.9

### 1. Formal closeout status
State clearly whether M8 is formally closed.

### 2. Milestone objective recap
Restate M8's objective in concise, exact language.

### 3. What was implemented
Summarize the implemented system changes, including:
- `obs8_deployment_v1`
- `obs_schema` selector
- `build_observation_obs8()`
- `ppo_hydrienv_v2`
- eval path for v2 vs v1 vs random
- new obs8 tests

### 4. Runtime result summary
Record the actual runtime result from M8.7, including:
- v2 mean return
- v1 mean return
- random mean return
- deployment gap %
- AC9 / AC10 / AC11 outcomes
- scenario classification

### 5. Methodology validity summary
State MVC-1 through MVC-4 status, including the timestep-budget deviation:
- what was fully satisfied
- what was partially satisfied
- why the 360k vs 500k deviation does or does not block closeout

### 6. What M8 now established
State what is now more true after M8.

At minimum capture:
- deployment-realistic obs8 control is viable in-simulation
- no negative deployment gap was observed under current placeholder-noise conditions
- reduced observation did not collapse policy performance
- obs8 currently outperformed obs14 under the executed training budget

### 7. What M8 does NOT establish
Preserve strict discipline here.

At minimum state that M8 does not establish:
- deployment readiness
- hardware validity
- calibrated realism
- sensor-only sufficiency
- that channels 0–5 are unnecessary
- that obs8 is globally superior to obs14
- that the deployment gap is "solved"

### 8. Step-budget deviation accounting
This section must explicitly address the fact that:
- spec target was 500k
- actual run was 360,448
- result is still valid but bounded
- whether M8 can close despite that deviation
- whether a full 500k rerun is recommended, optional, or required for M9

Do not ignore this point.

### 9. Protected-lineage confirmation
Confirm that:
- `ppo_hydrienv_v1` artifacts remained untouched
- CCE remained untouched
- M8 added a new canonical artifact without replacing prior ones

### 10. Achievements summary
Create a concise milestone achievement summary suitable for later reuse.

### 11. Next-milestone inheritance
State exactly what M9 now inherits from M8.

This should include:
- canonical obs8 deployment-realistic schema
- canonical v2 artifact under obs8
- positive deployment-gap result under placeholder noise
- open confounds still unresolved
- plausible M9 directions unlocked by M8

### 12. Recommended next milestone direction
Name the strongest next milestone candidate.

Do not over-elaborate, but do state the logic.

## Critical instructions

- Do not overclaim from the positive result
- Do not erase or soften the placeholder-noise confound
- Do not ignore the 360k-step deviation
- Do not let the closeout read like marketing
- Do not contradict M8.3 prohibited claims
- Keep the closeout authoritative and concise

## Required return

Return:
1. `docs/orchestration/M8_Foundation/M8.8_closeout_prompt.md`
2. `docs/orchestration/M8_Foundation/M8.9_closeout_record.md`
3. the insight file path created under `docs/orchestration/insights/M8_Foundation/`
4. short summary of what M8 formally established
5. the next recommended milestone direction
