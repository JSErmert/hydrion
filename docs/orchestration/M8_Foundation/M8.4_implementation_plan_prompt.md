# M8.4 — Implementation Plan Prompt

First, write this prompt content to:
`docs/orchestration/M8_Foundation/M8.4_implementation_plan_prompt.md`

If the folder path does not yet exist, create it first.

Then execute the task exactly as written below.

Operate under HydrOS x AlignFlow Orchestration Contract v3.1 at:
`docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing prerequisite artifact, or ambiguity that would risk milestone integrity.

## Objective

Create the implementation plan for:

**M8 — Deployment Gap Quantification / Privileged-Channel Ablation**

Write the output to:
`docs/orchestration/M8_Foundation/M8.5_implementation_plan.md`

Also create one required insight file for this pass at:
`docs/orchestration/insights/M8_Foundation/M8.4_insight_<short_slug>.md`

If the insights folder path does not yet exist, create it first.

After creating each markdown artifact, also print the full markdown in chat per Contract v3.1.

## Governing inputs

Use the following as binding inputs:

- `docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`
- `docs/orchestration/M8_Foundation/M8.1_refinement_output.md`
- `docs/orchestration/M8_Foundation/M8.1R.2_research_brief.md`
- `docs/orchestration/M8_Foundation/M8.1R.4_sources_map.md`
- `docs/orchestration/M8_Foundation/M8.3_execution_document.md`
- `docs/orchestration/insights/M8_Foundation/M8.0_insight_actuator_channels_misclassified_as_privileged.md`
- `docs/orchestration/insights/M8_Foundation/M8.1R.3_insight_source_map_defines_m8_claim_validity_boundary.md`
- `docs/orchestration/insights/M8_Foundation/M8.2_insight_execution_document_anchors_comparison_to_deployment_gap.md`

Use actual repo code state as source of truth where file paths, schemas, tests, and script names matter.

## Purpose

M8.5 must translate M8.3 into concrete implementation reality.

This is the file-by-file plan that defines:
- which files must be created or modified
- what exact changes each file must receive
- how to preserve benchmark lineage
- how to satisfy AC1–AC15
- how to verify each AC
- what execution order is safest

This is not the implementation execution prompt yet.

## Required implementation-planning focus

The implementation plan must turn the following M8.3 requirements into concrete steps:

- define and implement `obs8_deployment_v1`
- preserve the 3-class channel taxonomy
- create `ppo_hydrienv_v2` training/eval path
- fit a fresh VecNormalize stats file for v2
- compare v2 vs v1 vs random correctly
- keep M7 and CCE artifacts untouched
- ensure Section 9 interpretation rules survive into execution and closeout

## Required output contents for M8.5

### 1. Executive implementation strategy
State the implementation strategy in one concise paragraph.

### 2. File-by-file implementation plan
For each file to create or modify, state:
- exact path
- create vs modify
- purpose
- exact change required
- what must not be changed in that file

At minimum, assess whether the plan should involve:
- `hydrion/sensors/sensor_fusion.py`
- `hydrion/env.py`
- new `hydrion/train_ppo_hydrienv_v2.py`
- new `hydrion/eval_ppo_hydrienv_v2.py`
- new or updated tests
- model metadata output path(s)

### 3. Schema implementation plan
State exactly how `obs8_deployment_v1` will be realized in code.

The plan must answer:
- whether to add a selector in `sensor_fusion.py`
- whether `HydrionEnv` must expose a schema switch
- how `observation_space` shape is handled
- how v1 (`obs14_v1`) remains preserved
- how silent schema drift is prevented

### 4. Training-script plan
Define exactly what `train_ppo_hydrienv_v2.py` must do, including:
- schema lock assertion
- seed
- timestep target
- VecNormalize behavior
- metadata generation
- artifact output paths
- inherited hyperparameters from v1
- what must remain identical to v1 for matched-comparison validity

### 5. Evaluation-script plan
Define exactly what `eval_ppo_hydrienv_v2.py` must do, including:
- loading v2, v1, and random baseline correctly
- using correct VecNormalize per policy
- avoiding v1 VecNorm reuse for v2
- producing metrics needed for ACs
- recording the calibration confound statement
- recording the cross-schema deployment-gap framing
- producing diagnostics for actuator + sensor channel activity

### 6. Test plan
State what new tests are required.

At minimum include planning for:
- obs8 schema shape/order/range test
- protection of obs14 behavior
- protection of CCE behavior
- VecNorm path separation if testable
- metadata correctness if testable

If a dedicated test file is needed, name it explicitly.

### 7. Acceptance criteria verification checklist
Take AC1–AC15 from M8.3 and convert them into a concrete verification checklist.

For each AC, provide:
- what file(s) or command(s) verify it
- expected success condition
- any notes on interpretation

Do not re-derive the ACs; inherit them from M8.3.

### 8. Protected artifacts and no-touch list
Explicitly list what must remain untouched.

At minimum consider:
- `ppo_hydrienv_v1` artifacts
- CCE artifacts
- stale scripts that should remain excluded
- any M7 metadata files
- existing obs14 behavior except where explicitly extended or parameterized

### 9. Risks, confounds, and operational safeguards
Translate the M8.3 risks into implementation safeguards.

At minimum include:
- VecNorm confusion safeguard
- schema-mismatch safeguard
- manual training runtime note
- single-seed interpretation caution
- placeholder-noise confound propagation into execution artifacts

### 10. Execution order
Provide the safest step-by-step implementation order.

This should be a numbered checklist starting from the first file to inspect or create.

### 11. Ready-for-execution verdict
State clearly whether M8 is ready to move from implementation plan to execution prompt once M8.5 is accepted.

## Critical instructions

- Do not broaden scope beyond M8.3
- Do not alter M8.3 ACs or interpretation rules
- Do not accidentally weaken benchmark-lineage discipline
- Do not treat internal implementation choices as if they were literature-backed
- Prefer the narrowest correct implementation path
- If there are two implementation approaches, choose the one that most cleanly preserves obs14_v1 and avoids silent drift

## Required return

Return:
1. `docs/orchestration/M8_Foundation/M8.4_implementation_plan_prompt.md`
2. `docs/orchestration/M8_Foundation/M8.5_implementation_plan.md`
3. the insight file path created under `docs/orchestration/insights/M8_Foundation/`
4. short summary of the implementation strategy
5. whether M8 is ready for execution after M8.5
