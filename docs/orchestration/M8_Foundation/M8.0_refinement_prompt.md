# M8.0 — Refinement Prompt

First, write this prompt content to:
`docs/orchestration/M8_Foundation/M8.0_refinement_prompt.md`

If the folder path does not yet exist, create it first.

Then execute the task exactly as written below.

Operate under HydrOS x AlignFlow Orchestration Contract v3 at:
`docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.md`

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing artifact, or ambiguity that would risk milestone integrity.

## Objective

Refine and define the highest-leverage next milestone for HydrOS after M7 closeout, under the governance logic of Contract v3.

This is a milestone-selection and scoping prompt, not yet an implementation prompt.

Write the output to:
`docs/orchestration/M8_Foundation/M8.1_refinement_output.md`

Also create one required insight file for this pass at:
`docs/orchestration/insights/M8_Foundation/M8.0_insight_<short_slug>.md`

If the insights folder path does not yet exist, create it first.

## Context

HydrOS now has:

- M5 closed
- M6 closed
- M7 closed
- Contract v3 active in repo
- M7.8 insight already recorded
- a canonical HydrionEnv baseline under `obs14_v1`
- preserved CCE benchmark lineage
- explicit realism boundaries and accepted limitations

The current strategic question is not whether HydrOS can continue, but what M8 should prioritize first.

## Required task

Using the HydrOS x AlignFlow loop:

**See → Understand → Protect → Act → Stabilize → Insight / Improve**

determine the highest-leverage M8 direction and refine it into a clean, governed milestone starting point.

## Candidate M8 branches to evaluate

At minimum, evaluate these as possible first M8 branches:

1. **Privileged-information reduction / ablation**
   - test dependence on truth channels 0–9
   - establish reduced-privilege or sensor-only baseline behavior

2. **Reward shaping / mission alignment**
   - refine reward to better represent extraction goals and operating trade-offs

3. **Calibration-aware realism expansion**
   - replace placeholder sensor realism assumptions where calibration support exists or can be credibly bounded

4. **Frontend telemetry alignment**
   - expose truth_state vs sensor_state vs policy/action layers visually in the console

5. **Other**
   - only if supported by a stronger argument than the above options

## Required evaluation standard

For each candidate branch, assess:

- realism leverage
- interpretability leverage
- benchmark safety
- dependency readiness
- frontend/backend implications if relevant
- deployment relevance
- risk of producing impressive but misleading results
- how much future work it unlocks

Do not treat all branches as equal if they are not.

## Required output contents for M8.1

### 1. Current post-M7 system state
State what is now true after M7.

### 2. Decision framing
State what question M8 should answer first.

### 3. Candidate branch comparison
Compare the candidate M8 directions using explicit reasoning.

### 4. Highest-leverage choice
Name the chosen M8 direction and explain why it is highest leverage now.

### 5. What M8 should and should not attempt
Define scope boundaries clearly.

### 6. Proposed milestone identity
Provide the proposed M8 milestone title and short description.

### 7. Immediate next document
State what the next artifact after M8.1 should be
(for example: research brief, execution document, or another scoped artifact).

### 8. Carry-forward risks
State what must remain protected from M7 into M8.

### 9. Recommended insight title
Name the insight file created for this refinement pass.

## Critical instructions

- Do not jump to implementation
- Do not overclaim deployment relevance
- Do not blur HydrionEnv and CCE lineage
- Do not choose a milestone because it sounds ambitious; choose it because it most improves realism, interpretability, and future decision quality
- Be explicit if one branch is blocked by missing calibration or dependency conditions
- Prefer the most strategically clarifying milestone, not just the most technically flashy one

## Required return

Return:
1. `docs/orchestration/M8_Foundation/M8.0_refinement_prompt.md`
2. `docs/orchestration/M8_Foundation/M8.1_refinement_output.md`
3. the insight file path created under `docs/orchestration/insights/M8_Foundation/`
4. short summary of the chosen M8 direction
5. the next artifact that should follow M8.1
