# M8.6 — Execution Prompt

First, write this prompt content to:
`docs/orchestration/M8_Foundation/M8.6_execution_prompt.md`

If the folder path does not yet exist, create it first.

Then execute the task exactly as written below.

Operate under HydrOS x AlignFlow Orchestration Contract v3.1 at:
`docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing prerequisite artifact, or ambiguity that would risk milestone integrity.

## Objective

Execute M8 exactly as defined by:

- `docs/orchestration/M8_Foundation/M8.3_execution_document.md`
- `docs/orchestration/M8_Foundation/M8.5_implementation_plan.md`

This execution must implement:

**M8 — Deployment Gap Quantification / Privileged-Channel Ablation**

Write the execution report to:
`docs/orchestration/M8_Foundation/M8.7_execution_report.md`

Also create one required insight file for this pass at:
`docs/orchestration/insights/M8_Foundation/M8.6_insight_<short_slug>.md`

If the insights folder path does not yet exist, create it first.

After creating each markdown artifact, also print the full markdown in chat per Contract v3.1.

## Binding instructions

M8.5 is now the controlling implementation document.
Do not redesign M8.
Do not broaden M8.
Do not reinterpret ACs.
Do not change M8.3 methodology validity conditions.
Do not weaken the four-scenario interpretation table.
Do not blur deployment-gap measurement with policy improvement.

## Required execution scope

Execute the code changes and validations defined in M8.5, including:

1. `hydrion/sensors/sensor_fusion.py`
   - append `build_observation_obs8()`
   - do not modify existing `build_observation()`

2. `hydrion/env.py`
   - add `build_observation_obs8` import
   - add `obs_schema: str = "obs14_v1"` parameter
   - add `_VALID_SCHEMAS` validation logic
   - parameterize `observation_space.shape`
   - dispatch `_observe()` based on schema

3. Create:
   - `hydrion/train_ppo_hydrienv_v2.py`
   - `hydrion/eval_ppo_hydrienv_v2.py`
   - `tests/test_obs8_schema.py`

4. Run verification in the order defined by M8.5 Section 10.

## Critical execution requirements

### 1. Preserve protected artifacts
Do not modify:
- `ppo_hydrienv_v1` artifacts
- CCE artifacts
- `build_observation()` existing logic
- existing obs14 default behavior
- reward logic
- physics pipeline

### 2. Enforce methodology validity conditions
Must preserve:
- MVC-1: fresh VecNormalize fit for v2
- MVC-2: matched training conditions
- MVC-3: calibration confound naming
- MVC-4: cross-schema deployment-gap framing

### 3. Run tests at both checkpoints
You must run:
- the full test suite after F1/F2 env/schema changes
- the full test suite again after all M8 code artifacts are created

### 4. Training runtime rule
`train_ppo_hydrienv_v2.py` must be run as a **manual foreground terminal command**.
Do not use a background process.
The environment kills background jobs around ~5 minutes, while training takes ~15–16 minutes.

### 5. AC11 halt rule
If evaluation shows `ppo_hydrienv_v1` return is not within 5% of the M7 canonical value (694.639),
halt interpretation of deployment-gap results and report that AC11 failed.
Do not overinterpret v2 vs v1 if AC11 fails.

## Required execution order

Follow M8.5 Section 10 exactly.

At minimum, execution must include:

### Phase A — code implementation
- implement F1–F5
- verify import integrity
- verify schema lock logic
- verify test file creation

### Phase B — regression validation
- run `pytest tests/`
- run `pytest tests/test_obs8_schema.py -v`
- confirm no regression to obs14 or CCE

### Phase C — artifact commit point
- commit code changes before training if appropriate

### Phase D — training
Run:
`python -m hydrion.train_ppo_hydrienv_v2`

Expected outputs:
- `models/ppo_hydrienv_v2.zip`
- `models/ppo_hydrienv_v2_vecnorm.pkl`
- `models/ppo_hydrienv_v2_meta.json`

### Phase E — evaluation
Run:
`python -m hydrion.eval_ppo_hydrienv_v2`

Must capture:
- v2 mean/std return
- v1 mean/std return
- random baseline mean/std return
- AC9, AC10, AC11 results
- calibration confound statement
- scenario classification under M8.3 Section 9

### Phase F — close execution state
- commit `ppo_hydrienv_v2_meta.json`
- write M8.7 execution report
- create insight file
- return exact artifact paths and status

## Required contents of M8.7 execution report

### 1. Final status
Was M8 execution completed successfully?

### 2. Files changed
List all created/modified files and artifact outputs.

### 3. Training result
Record:
- actual timesteps
- seed
- schema
- runtime behavior
- artifact creation

### 4. Evaluation result
Record:
- v2 vs v1 vs random metrics
- deployment gap %
- AC9 / AC10 / AC11 outcome

### 5. Methodology validity check
State whether MVC-1 through MVC-4 were satisfied.

### 6. Four-scenario classification
Identify which scenario from M8.3 Section 9 was triggered.

### 7. Prohibited-claims compliance
State explicitly what M8 does not prove.

### 8. Ready-for-closeout verdict
State whether M8 is ready for formal closeout after M8.7.

## Required insight contents

The M8.6 insight file should capture:
- what execution revealed that planning could not
- whether the additive schema-selector approach held up cleanly
- whether the deployment-gap framing survived implementation intact
- what the strongest immediate carry-forward implication is

## Required return

Return:
1. `docs/orchestration/M8_Foundation/M8.6_execution_prompt.md`
2. `docs/orchestration/M8_Foundation/M8.7_execution_report.md`
3. the insight file path created under `docs/orchestration/insights/M8_Foundation/`
4. short summary of the execution outcome
5. whether M8 is ready for formal closeout
