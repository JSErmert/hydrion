# M10.1A.1 — Refinement Correction Patch

First, write this prompt content to:

docs/orchestration/M10_Physics/M10.1A_refinement_correction_prompt.md

Then execute exactly as written below.

---

## Objective

Apply a **minimal corrective patch** to the M10.1 refinement so implementation can proceed safely.

This is NOT a new refinement pass.
This is NOT implementation.
This is a **correction layer** responding to the blocking findings in:

- `docs/orchestration/M10_Physics/M10.1_validation_patch.md`

The correction must fix exactly two issues:

1. **Stokes settling double-count** under `capture_mode == "m5_rt"`
2. **Observation coupling omission** via `C_out → optical.py → obs8 indices 4-5`

Do not expand scope beyond these two corrections.

---

## Inputs

- `docs/orchestration/M10_Physics/M10.1_refinement_output.md`
- `docs/orchestration/M10_Physics/M10.1_validation_patch.md`
- `docs/orchestration/insights/M10_Physics/M10.0_insight_capture_is_reward_decoupled.md`

---

## Required Outputs

### Output 1
Write:

`docs/orchestration/M10_Physics/M10.1A_refinement_correction.md`

### Output 2
Revise in place:

`docs/orchestration/M10_Physics/M10.1_refinement_output.md`

### Output 3
Revise in place:

`docs/orchestration/insights/M10_Physics/M10.0_insight_capture_is_reward_decoupled.md`

---

## Required Corrections

---

### 1. Stokes Settling Guard (BLOCKING)

The correction must explicitly state:

- under `capture_mode == "m5_rt"`, `capture_boost_settling = 0.0`
- the separate `stokes_velocity_ms()` additive settling term must not contribute under M5 RT mode
- reason: RT already includes gravity via `N_G`; otherwise settling is double-counted

This must appear in:

- correction doc
- revised refinement output
- implementation-readiness section

---

### 2. Observation Coupling Correction (BLOCKING)

The correction must explicitly state:

- M10 capture integration is **reward-decoupled**
- but **not observation-decoupled**
- `C_out` changes under M5 RT mode
- `optical.py` reads `C_out`
- therefore:
  - `sensor_turbidity`
  - `sensor_scatter`
  - obs8 indices **4 and 5**
  are expected to differ between M4 and M5 modes

This must replace any claim that capture is "telemetry only" or that obs8 is unaffected.

---

### 3. Evaluation Plan Patch

Revise the M10.1 evaluation logic to distinguish:

#### Must match between M4 and M5
- reward trajectory
- clogging trajectory
- hydraulics trajectory
- obs8 indices 0-3
- obs8 indices 6-7

#### Expected to differ
- `C_out`
- `sensor_turbidity`
- `sensor_scatter`
- obs8 indices 4-5
- particle capture diagnostics

This must be written explicitly.

---

### 4. Success Criteria Patch

Success criteria must be updated so that:

- difference in obs8 indices 4-5 is NOT treated as failure
- difference in reward trajectory IS treated as failure
- difference in clogging trajectory IS treated as failure
- presence of Stokes settling boost under `m5_rt` IS treated as failure

---

### 5. Insight Patch

Revise the M10.0 insight so it no longer says or implies:

> capture is invisible to the policy

Instead it should say:

> capture is invisible to reward, but partially visible to policy through the optical sensor pathway

Keep the insight otherwise intact. Do not rewrite its whole argument.

---

## Constraints

- No implementation
- No new subsystem decisions
- No scope expansion
- No new architecture changes
- Do not reopen subsystem selection
- Be minimal and exact

---

## Required Return

Return:

1. correction document path
2. confirmation that M10.1 refinement output was revised
3. confirmation that M10.0 insight was revised
4. corrected one-sentence system statement for M10 Phase 1
5. readiness verdict after patch: READY or NOT READY
