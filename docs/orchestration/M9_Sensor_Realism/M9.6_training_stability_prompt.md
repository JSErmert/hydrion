# M9.6 — Training Stability Recovery Pass

First, write this prompt content to:

`docs/orchestration/M9_Foundation/M9.6_training_stability_prompt.md`

Then execute exactly as written below.

Operate under:

- docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md
- docs/orchestration/governance/HydrOS_Orchestrator_Execution_Schema_v1.md
- CLAUDE.md

Binding input:
- M9.3_execution_document.md
- M9.5_execution_report.md

---

## Objective

Recover PPO convergence under calibrated sensor noise **without changing system architecture or scope**.

This is a **controlled hyperparameter correction pass**, not a redesign.

---

## Scope (STRICT)

### Allowed changes:
- PPO hyperparameters only
- training duration

### Forbidden:
- NO changes to:
  - sensor parameters
  - reward function
  - observation schema
  - physics layer
  - config isolation
  - VecNormalize structure
  - SP1–SP6 protections

---

## Hypothesis

M9 failure was caused by:

- loss of temporal correlation (dp drift removal)
- PPO instability under cleaner signal
- overly aggressive updates (clip_fraction ~ 0.34)

---

## Required Experiments

Run **exactly 2 training configurations**:

---

### Config A — Conservative PPO

- learning_rate = **1e-4**
- clip_range = **0.1**
- batch_size = **128**
- n_epochs = **15**
- steps = **500k**

Purpose:
-> stabilize updates and allow value function to fit

---

### Config B — Stability + Data

- learning_rate = **3e-5**
- clip_range = **0.1**
- batch_size = **256**
- n_epochs = **20**
- steps = **1,000,000**

Purpose:
-> compensate for loss of implicit temporal signal

---

## Training Rules

- Use `configs/m9_calibrated.yaml`
- Fresh VecNormalize for each run
- New artifact names:
  - `ppo_hydrienv_v2_cal_A`
  - `ppo_hydrienv_v2_cal_B`
- NEVER reuse previous vecnorm

---

## Mandatory Checks

For each run:

### 1. Convergence signals
- explained_variance > 0.3
- clip_fraction < 0.2
- stable reward curve

### 2. AC11 verification (unchanged)
Run v1 baseline:
-> must equal 694.639 exactly

---

## Evaluation

Compare:

- v2_cal_A vs v1
- v2_cal_B vs v1
- vs random baseline

Apply four-scenario classification (unchanged)

---

## Success Criteria

Recovery is achieved if:

- model return > random (~584)
- training converges (explained_variance positive)
- no SP violations
- stable policy behavior (non-degenerate)

---

## Failure Condition

If BOTH configs fail:

-> conclude:

> PPO + MLP is insufficient under calibrated noise

Do NOT continue hyperparameter tuning beyond these two runs.

Escalate to:

- recurrent policy (LSTM PPO)
- or architecture-level change (M10 scope)

---

## Output

Write:

`docs/orchestration/M9_Foundation/M9.6_training_stability_report.md`

Include:

- both configs
- convergence metrics
- returns
- scenario classification
- recommendation:
  - recovered -> proceed to closeout
  - not recovered -> escalate architecture

---

## Required Return

Return:

1. training report path
2. summary of both runs
3. best-performing configuration
4. final recommendation:
   - close M9
   - or escalate to architecture change
