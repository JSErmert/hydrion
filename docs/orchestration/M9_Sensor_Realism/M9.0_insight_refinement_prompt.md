# M9.0 Insight Refinement Prompt — AROD Validation Lock-In

First, write this prompt content to:
`docs/orchestration/M9_Foundation/M9.0_insight_refinement_prompt.md`

If the folder path does not yet exist, create it first.

Then execute the task exactly as written below.

Operate under both governing documents:

- `docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`
- `docs/orchestration/governance/HydrOS_Orchestrator_Execution_Schema_v1.md`

## Objective

Refine and strengthen the existing M9.0 insight artifact so it more clearly captures both:

1. the milestone-level strategic learning from the M9 refinement pass
2. the workflow-level AROD validation learning from the first live use of AROD in milestone refinement

Do **not** create a new insight file.
Instead, revise the existing insight file in place:

`docs/orchestration/insights/M9_Foundation/M9.0_insight_sensor_calibration_is_the_decisive_m9_move.md`

Also preserve the existing refinement output file:
`docs/orchestration/M9_Foundation/M9.1_refinement_output.md`

This pass is about improving the insight, not changing the chosen M9 direction.

After revising the markdown artifact, also print the full revised markdown in chat as a single fenced code block per current HydrOS orchestration practice.

## Governing inputs

Use the following as binding inputs:

- `docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`
- `docs/orchestration/governance/HydrOS_Orchestrator_Execution_Schema_v1.md`
- `docs/orchestration/M9_Foundation/M9.1_refinement_output.md`
- `docs/orchestration/insights/M9_Foundation/M9.0_insight_sensor_calibration_is_the_decisive_m9_move.md`

Use actual repo state as source of truth.

## Required revision goals

### 1. Preserve the core conclusion
Keep the core conclusion unchanged:
- sensor calibration is the decisive M9 move
- not because it is the most exciting branch
- but because it is the prerequisite for the interpretability of the other branches

### 2. Strengthen the prerequisite logic
Make more explicit that:
- domain randomization depends on calibration
- obs4 ablation depends on calibration
- frontend/telemetry work depends on calibration for visual validity
- step-matched rerun improves precision, not interpretability
- distillation is premature until the calibrated-noise result is known

This "prerequisite before branch" logic should read as the deepest lesson of the pass.

### 3. Add an explicit AROD validation subsection
Add a dedicated subsection near the end titled:

## AROD Validation Note

This subsection must clearly state:

- **AROD elements applied in this pass**
  - confound-priority structure
  - visual validity bridge
  - system-model change requirement

- **How AROD improved the refinement**
  - what became clearer because of it
  - what decision quality improved because of it

- **Whether AROD added friction**
  - if none, say so explicitly
  - if any slight overlap or redundancy appeared, state it honestly

- **Carry-forward decision**
  - keep as-is
  - keep with minor refinement
  - revise before next pass

### 4. Make this insight more reusable
The revised insight should be useful later for:
- M9 closeout
- AROD v1 evaluation
- possible AROD v1.1 refinement

So write it not just as a one-pass summary, but as a reusable learning artifact.

### 5. Do not weaken discipline
Do not:
- soften the confound structure
- blur milestone learning with deployment claims
- rewrite the milestone choice
- add implementation details that belong to research or execution documents

## Style requirements

- sharper than the current version
- slightly more structured
- high-signal
- clearly split between:
  - strategic milestone learning
  - orchestration / AROD learning

## Required return

Return:
1. `docs/orchestration/M9_Foundation/M9.0_insight_refinement_prompt.md`
2. confirmation that `docs/orchestration/insights/M9_Foundation/M9.0_insight_sensor_calibration_is_the_decisive_m9_move.md` was revised in place
3. short summary of what was strengthened in the insight
4. recommendation on whether AROD should be kept unchanged, kept with minor refinement, or revised before M9.1R.1
