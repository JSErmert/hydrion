# M9.4 — Execution Prompt: Sensor Calibration Implementation

First, write this prompt content to:

`docs/orchestration/M9_Foundation/M9.4_execution_prompt.md`

If the folder path does not yet exist, create it first.

Then execute the task exactly as written below.

Operate under:

- `docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`
- `docs/orchestration/governance/HydrOS_Orchestrator_Execution_Schema_v1.md`
- `CLAUDE.md`

Use the following binding inputs:

- `docs/orchestration/M9_Foundation/M9.3_execution_document.md`
- `docs/orchestration/M9_Foundation/M9.1R.3_verified_source_brief.md`
- `docs/orchestration/M9_Foundation/M9.1R.4_sources_map.md`
- `docs/orchestration/M9_Foundation/M9.2_pre_execution_readiness_synthesis.md`
- `docs/orchestration/M9_Foundation/M9.2A_G1_sensor_class_resolution.md`

---

## Objective

Execute M9 exactly as specified in M9.3.

This pass must:

1. implement the calibrated sensor-noise configuration in an isolated config
2. restore optical state-separation compliance
3. preserve all protected lineage and AC11 exact-match guarantees
4. train the new calibrated obs8 artifact
5. evaluate it under the binding four-scenario interpretation structure
6. write the M9.5 execution report

This is an execution pass, not a redesign pass.

---

## Runtime class

`autonomous_until_manual_runtime`

Training may exceed the 600-second autonomous bash ceiling. If so, execution must stop at the manual-runtime boundary and return the exact foreground command the user must run, along with the required post-runtime continuation instructions.

---

## Binding scope

### Included

- Create `configs/m9_calibrated.yaml`
- Apply calibrated values from M9.3 Section 4
- Remove the `optical.py` → `truth_state` mirror (SP6)
- Re-run full tests
- Re-run AC11 checkpoints as specified in M9.3
- Create calibrated training and eval scripts if not already present
- Train `ppo_hydrienv_v2_cal`
- Evaluate calibrated artifact under the M9.3 evaluation plan
- Write `M9.5_execution_report.md`

### Excluded

- No edits to `configs/default.yaml`
- No edits to physics-layer models
- No edits to reward function
- No edits to obs schema shape
- No edits to optical gain coefficients
- No camera-device work
- No M5 integration work
- No governance expansion
- No interpretation beyond M9.3 prohibited-claims limits

---

## Required execution sequence

### Phase 1 — Pre-execution verification

1. Run AC11-1 against `configs/default.yaml`
   - expected exact return: **694.639**
   - if mismatch: HALT immediately

2. Create `configs/m9_calibrated.yaml`
   - copy `configs/default.yaml`
   - change only:
     - `dp_drift_rate_kPa_per_step`
     - `dp_drift_max_kPa`
     - `dp_fouling_gain`
     - `turbidity_noise_std`
     - `scatter_noise_std`

3. Run AC11-2 against `configs/default.yaml`
   - expected exact return: **694.639**
   - if mismatch: HALT immediately

4. Remove the `optical.py` → `truth_state` mirror (SP6)

5. Run the full test suite
   - expected: **128/128**
   - if regression occurs: investigate the actual dependency and root cause; do not restore the mirror merely to silence the regression

6. Ensure calibrated training/eval scripts exist:
   - `hydrion/train_ppo_hydrienv_v2_cal.py`
   - `hydrion/eval_ppo_hydrienv_v2_cal.py`

7. Run AC11-3 against `configs/default.yaml`
   - expected exact return: **694.639**
   - if mismatch: HALT immediately

---

### Phase 2 — Training

Train the new calibrated artifact:

- algorithm: PPO
- schema: `obs8_deployment_v1`
- seed: 42
- total timesteps: **360000**
- config: `configs/m9_calibrated.yaml`
- fresh VecNormalize only
- artifact name: `ppo_hydrienv_v2_cal`

Expected outputs:

- `models/ppo_hydrienv_v2_cal.zip`
- `models/ppo_hydrienv_v2_cal_vecnorm.pkl`
- `models/ppo_hydrienv_v2_cal_meta.json`

If training completes within the autonomous runtime ceiling, continue automatically.

If training does not complete within the autonomous runtime ceiling, stop and return:

### Manual runtime required

Run exactly:

```bash
python -m hydrion.train_ppo_hydrienv_v2_cal --steps 360000
```

After completion, paste the training output.
Then execution continues with Phase 3.

---

### Phase 3 — Evaluation

1. Run evaluation script
2. Record:
   - v2_cal mean return ± std
   - v1 mean return (must equal 694.639)
   - random mean return
   - deployment gap %
   - four-scenario classification
   - all AC checks

3. Run AC11-4 against `configs/default.yaml`
   - expected exact return: **694.639**

---

### Phase 4 — Report

Write:

`docs/orchestration/M9_Foundation/M9.5_execution_report.md`

Include:
- all results
- all AC checks
- four-scenario classification
- prohibited claims preserved
- confound status
- next artifact

---

## Required return

Return:

1. `docs/orchestration/M9_Foundation/M9.4_execution_prompt.md`
2. `docs/orchestration/M9_Foundation/M9.5_execution_report.md` (or PENDING if manual runtime required)
3. all artifact paths created
4. AC11 status at each checkpoint
5. next artifact after M9.5
