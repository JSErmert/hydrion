# M9.0 — Refinement Prompt

First, write this prompt content to:
`docs/orchestration/M9_Foundation/M9.0_refinement_prompt.md`

If the folder path does not yet exist, create it first.

Then execute the task exactly as written below.

Operate under both governing documents:

- `docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`
- `docs/orchestration/governance/HydrOS_Orchestrator_Execution_Schema_v1.md`

Do not ask for confirmation or permissions.
Proceed autonomously unless you encounter a true architectural conflict, missing prerequisite artifact, or ambiguity that would risk milestone integrity.

## Objective

Refine and define the highest-leverage next milestone for HydrOS after M8 closeout.

This is a milestone-selection and scoping prompt, not yet an implementation prompt.

Write the output to:
`docs/orchestration/M9_Foundation/M9.1_refinement_output.md`

Also create one required insight file for this pass at:
`docs/orchestration/insights/M9_Foundation/M9.0_insight_<short_slug>.md`

If the insights folder path does not yet exist, create it first.

After creating each markdown artifact, also print the full markdown in chat as a single fenced code block per current HydrOS orchestration practice.

## Runtime class

`fully_autonomous`

This pass is milestone refinement and candidate evaluation only.
No manual runtime step should be required in this pass.

## Governing inputs

Use the following as binding inputs:

- `docs/orchestration/governance/HydrOS_x_AlignFlow_Orchestration_Contract_v3.1.md`
- `docs/orchestration/governance/HydrOS_Orchestrator_Execution_Schema_v1.md`
- `docs/orchestration/M8_Foundation/M8_achievements.md`
- `docs/orchestration/M8_Foundation/M8.9_closeout_record.md`
- `docs/orchestration/M8_Foundation/M8.7_execution_report.md`
- all M8 insight files currently stored in:
  `docs/orchestration/insights/M8_Foundation/`

Use actual repo state as source of truth where artifact names, schemas, and policies matter.

## Context

HydrOS now has:

- M5 closed
- M6 closed
- M7 closed
- M8 closed
- Contract v3.1 active in repo
- AROD Schema v1 active in repo
- canonical HydrionEnv policies:
  - `ppo_hydrienv_v1` on `obs14_v1`
  - `ppo_hydrienv_v2` on `obs8_deployment_v1`
- preserved CCE lineage
- canonical three-class channel taxonomy:
  - `0–5` physics_truth
  - `6–9` actuator_feedback
  - `10–13` sensor_derived

M8 established:
- obs8 deployment-realistic control is viable in-simulation
- no negative deployment gap was observed under current placeholder-noise conditions
- the primary unresolved confound is placeholder sensor noise
- step-budget asymmetry and single-seed evidence remain secondary confounds

## Visual validity bridge

M9 refinement must explicitly assume that M6 and beyond are not only backend realism milestones.

HydrOS is ultimately evolving toward a **scientifically grounded visual simulation console**.

Therefore, M9 candidate evaluation must consider not only realism, control, and confound reduction, but also whether the milestone improves the future console's **visual validity**.

For each candidate branch, explicitly assess whether it improves:
- future truth-vs-sensor visualization fidelity
- the scientific meaning of displayed telemetry
- the legitimacy of future policy-view rendering
- the legitimacy of future action-trace rendering
- the reduction of confounds that would otherwise make a future visual console misleading

Do not treat this as secondary decoration. Treat it as part of strategic milestone leverage.

## Required task

Using the HydrOS x AlignFlow loop:

**See → Understand → Protect → Act → Stabilize → Insight / Improve**

and using AROD Schema v1 as the orchestration structure, determine the highest-leverage M9 direction and refine it into a clean, governed milestone starting point.

## Required AROD application for this pass

Apply the following AROD structures explicitly in M9.1:

### 1. Confound-priority ranking
Rank inherited confounds as:
- primary
- secondary
- tertiary

State clearly which confound M9 should reduce first and why.

### 2. System-model change requirement
State:
- what changed in the system model after M8
- what assumption M9 is most likely to clarify or correct
- what future milestones must no longer assume if M9 succeeds

### 3. Visual-validity relevance
For each candidate branch, state whether it materially improves future visual validity and why.

### 4. AROD validation note
Briefly state whether this refinement pass suggests that AROD Schema v1 is helping milestone selection quality, or whether any parts feel unnecessary or overly rigid.

Do not over-elaborate this. One compact section is enough.

## Candidate M9 branches to evaluate

At minimum, evaluate these as possible first M9 branches:

### 1. Sensor calibration / realism tightening
Replace placeholder sensor-noise parameters with grounded, literature-supported or device-supported values for:
- `dp_drift_rate`
- `dp_fouling_gain`
- `flow_calibration_bias_std`

Evaluate whether this is the highest-leverage next move because it directly addresses M8's primary confound.

### 2. Domain randomization over calibrated sensor ranges
After or alongside calibration, define variability ranges for sensor-noise parameters and test robustness of deployment-gap results across realistic regimes.

### 3. Step-matched v2 benchmark completion
Run a full 500k-step `ppo_hydrienv_v2` training path to eliminate the 360k vs 500k asymmetry and strengthen benchmark precision before deeper realism work.

### 4. Distillation / teacher-student follow-on
Use `ppo_hydrienv_v1` as teacher and design a distillation-style milestone if the observation-regime transition should be improved through privileged-to-deployment transfer.

### 5. obs4_sensor_only ablation
Test the stricter 4D sensor-only scenario (`10–13` only) to isolate the value of actuator feedback channels `6–9`.

### 6. Frontend / telemetry milestone
Assess whether a visual truth-vs-sensor-vs-policy milestone is now timely, or whether realism/confound reduction still has higher leverage first.

### 7. Other
Only if supported by a stronger argument than the above options.

## Required evaluation standard

For each candidate branch, assess:

- realism leverage
- confound reduction leverage
- benchmark safety
- interpretation leverage
- dependency readiness
- implementation tractability
- visual validity leverage
- frontend/backend implications if relevant
- deployment relevance
- risk of producing an impressive but misleading result
- how much future work it unlocks

Do not treat all branches as equal if they are not.

## Required output contents for M9.1

### 1. Current post-M8 system state
State what is now true after M8.

### 2. Decision framing
State the core strategic question M9 should answer first.

### 3. Inherited confounds
Rank inherited confounds as:
- primary
- secondary
- tertiary

State which one M9 should reduce first.

### 4. Candidate branch comparison
Compare the candidate M9 directions using explicit reasoning.

### 5. Visual validity relevance by branch
For each major candidate, state whether it improves future visual validity and how strongly.

### 6. Highest-leverage choice
Name the chosen M9 direction and explain why it is highest leverage now.

### 7. What M9 should and should not attempt
Define scope boundaries clearly.

### 8. Proposed milestone identity
Provide the proposed M9 milestone title and short description.

### 9. System-model change target
State:
- what assumption M9 is expected to clarify or correct
- what future milestones would understand differently if M9 succeeds

### 10. AROD validation note
State briefly whether using AROD Schema v1 improved this refinement pass and how.

### 11. Immediate next document
State what the next artifact after M9.1 should be
(for example: research brief, source map, or execution document).

### 12. Carry-forward risks
State what must remain protected from M8 into M9.

### 13. Recommended insight title
Name the insight file created for this refinement pass.

## Critical instructions

- Do not jump to implementation
- Do not weaken M8's prohibited-claims discipline
- Do not choose a milestone because it sounds ambitious; choose it because it most improves realism, confound reduction, future decision quality, and visual validity
- Be explicit if one branch depends on calibration data, literature support, or a benchmark rerun
- Prefer the move that most directly improves the meaning of the M8 result
- Treat visual/frontend work as strategically valuable but not automatically highest leverage
- Keep the refinement output decisive, not indecisive

## Required return

Return:
1. `docs/orchestration/M9_Foundation/M9.0_refinement_prompt.md`
2. `docs/orchestration/M9_Foundation/M9.1_refinement_output.md`
3. the insight file path created under `docs/orchestration/insights/M9_Foundation/`
4. short summary of the chosen M9 direction
5. the next artifact that should follow M9.1
