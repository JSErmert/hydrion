export default function HydrosLiveDashboardConcept() {
  const stages = [
    { name: "Stage 1 · Coarse", area: "120 cm²", fouling: 38, dp: 8.2, status: "Stable" },
    { name: "Stage 2 · Medium", area: "220 cm²", fouling: 54, dp: 14.7, status: "Watch" },
    { name: "Stage 3 · Fine Pleated", area: "900 cm²", fouling: 71, dp: 31.4, status: "Maintenance" },
  ];

  const calibration = [
    { label: "Hydraulics", state: "Grounded", note: "Washer bounds + point-of-use flow/pressure studies" },
    { label: "Clogging", state: "Proxy-Constrained", note: "Nonlinear structure supported; exact weights still provisional" },
    { label: "Backflush", state: "Proxy-Constrained", note: "Pulse recovery logic supported; effluent performance unverified" },
    { label: "Sensors", state: "Partially Grounded", note: "Pressure/flow strong, optical still provisional" },
    { label: "Electrostatics", state: "Staged for M2", note: "Mechanism supported, device-scale gain not yet bench-verified" },
  ];

  const events = [
    { t: "00:00", event: "Cycle start", detail: "Laundry outflow enters swirl-conditioned inlet" },
    { t: "00:18", event: "Nominal processing", detail: "Flow stabilizes near 13.6 L/min" },
    { t: "01:06", event: "Fouling threshold crossed", detail: "Stage 3 exceeds 70% equivalent loading" },
    { t: "01:12", event: "Backflush burst", detail: "Pulse sequence initiated: 3 pulses / 0.4 s" },
    { t: "01:24", event: "Partial recovery", detail: "Pressure reduced, residual fouling remains" },
    { t: "01:40", event: "Bypass avoided", detail: "System remains below overload threshold" },
  ];

  const pill = (state) => {
    if (state === "Grounded") return "bg-emerald-100 text-emerald-700 border-emerald-200";
    if (state === "Partially Grounded") return "bg-lime-100 text-lime-700 border-lime-200";
    if (state === "Proxy-Constrained") return "bg-amber-100 text-amber-700 border-amber-200";
    if (state === "Staged for M2") return "bg-sky-100 text-sky-700 border-sky-200";
    if (state === "Maintenance") return "bg-red-100 text-red-700 border-red-200";
    if (state === "Watch") return "bg-yellow-100 text-yellow-700 border-yellow-200";
    return "bg-slate-100 text-slate-700 border-slate-200";
  };

  return (
    <div className="min-h-screen bg-slate-50 text-slate-900 p-6 md:p-8">
      <div className="max-w-7xl mx-auto space-y-6">
        <div className="grid lg:grid-cols-3 gap-6">
          <div className="lg:col-span-2 rounded-3xl bg-white shadow-sm border border-slate-200 p-6">
            <div className="flex items-start justify-between gap-4">
              <div>
                <p className="text-sm uppercase tracking-[0.2em] text-slate-500">HydrOS</p>
                <h1 className="text-3xl md:text-4xl font-semibold tracking-tight mt-1">Live Dashboard Concept</h1>
                <p className="mt-3 text-slate-600 max-w-3xl">
                  A visual operating view for Hydrion that blends simulation state, calibration trust, maintenance timing,
                  and implementation-readiness into one interface.
                </p>
              </div>
              <div className="rounded-2xl border border-slate-200 px-4 py-3 bg-slate-50 min-w-[170px]">
                <div className="text-xs text-slate-500">Run Mode</div>
                <div className="text-lg font-medium mt-1">Scenario Playback</div>
                <div className="text-sm text-slate-600 mt-1">Laundry outflow · fiber-heavy</div>
              </div>
            </div>

            <div className="grid md:grid-cols-4 gap-4 mt-6">
              {[
                ["Processed Flow", "13.6 L/min", "Nominal band"],
                ["Total ΔP", "54.3 kPa", "Rising but safe"],
                ["Mesh Loading Avg", "54%", "Recovery advisable soon"],
                ["Backflush State", "Armed", "Cooldown clear"],
              ].map(([k, v, s]) => (
                <div key={k} className="rounded-2xl bg-slate-50 border border-slate-200 p-4">
                  <div className="text-xs text-slate-500">{k}</div>
                  <div className="text-2xl font-semibold mt-2">{v}</div>
                  <div className="text-sm text-slate-600 mt-1">{s}</div>
                </div>
              ))}
            </div>
          </div>

          <div className="rounded-3xl bg-white shadow-sm border border-slate-200 p-6">
            <div className="flex items-center justify-between">
              <h2 className="text-lg font-semibold">Calibration Trust Stack</h2>
              <span className="text-xs rounded-full px-3 py-1 border border-slate-200 bg-slate-50 text-slate-600">M1.5 → M2</span>
            </div>
            <div className="mt-4 space-y-3">
              {calibration.map((item) => (
                <div key={item.label} className="rounded-2xl border border-slate-200 p-4">
                  <div className="flex items-center justify-between gap-3">
                    <div className="font-medium">{item.label}</div>
                    <span className={`text-xs rounded-full px-2.5 py-1 border ${pill(item.state)}`}>{item.state}</span>
                  </div>
                  <p className="text-sm text-slate-600 mt-2">{item.note}</p>
                </div>
              ))}
            </div>
          </div>
        </div>

        <div className="grid xl:grid-cols-5 gap-6">
          <div className="xl:col-span-3 rounded-3xl bg-white shadow-sm border border-slate-200 p-6">
            <div className="flex items-center justify-between gap-4">
              <div>
                <h2 className="text-xl font-semibold">Tri-Stage Filtration State</h2>
                <p className="text-slate-600 mt-1">Operational view of stage load, pressure burden, and maintenance posture.</p>
              </div>
              <div className="text-sm text-slate-500">Hydrion physics backbone</div>
            </div>

            <div className="mt-6 grid gap-4">
              {stages.map((stage) => (
                <div key={stage.name} className="rounded-2xl border border-slate-200 p-5">
                  <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
                    <div>
                      <div className="font-semibold text-lg">{stage.name}</div>
                      <div className="text-sm text-slate-500 mt-1">Effective area: {stage.area}</div>
                    </div>
                    <span className={`text-xs rounded-full px-3 py-1 border self-start ${pill(stage.status)}`}>{stage.status}</span>
                  </div>

                  <div className="grid md:grid-cols-2 gap-5 mt-4">
                    <div>
                      <div className="flex items-center justify-between text-sm mb-2">
                        <span className="text-slate-600">Equivalent Fouling</span>
                        <span className="font-medium">{stage.fouling}%</span>
                      </div>
                      <div className="h-3 rounded-full bg-slate-100 overflow-hidden">
                        <div className="h-full rounded-full bg-slate-800" style={{ width: `${stage.fouling}%` }} />
                      </div>
                    </div>
                    <div>
                      <div className="flex items-center justify-between text-sm mb-2">
                        <span className="text-slate-600">Stage ΔP</span>
                        <span className="font-medium">{stage.dp} kPa</span>
                      </div>
                      <div className="h-3 rounded-full bg-slate-100 overflow-hidden">
                        <div className="h-full rounded-full bg-slate-500" style={{ width: `${Math.min(stage.dp * 2, 100)}%` }} />
                      </div>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>

          <div className="xl:col-span-2 rounded-3xl bg-white shadow-sm border border-slate-200 p-6">
            <div className="flex items-center justify-between gap-4">
              <div>
                <h2 className="text-xl font-semibold">Run Narrative</h2>
                <p className="text-slate-600 mt-1">The simulation should feel like a machine, not just a table of values.</p>
              </div>
            </div>
            <div className="mt-6 space-y-4">
              {events.map((item) => (
                <div key={item.t} className="grid grid-cols-[64px_1fr] gap-4">
                  <div className="text-sm font-medium text-slate-500 pt-0.5">{item.t}</div>
                  <div className="rounded-2xl border border-slate-200 p-4">
                    <div className="font-medium">{item.event}</div>
                    <div className="text-sm text-slate-600 mt-1">{item.detail}</div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>

        <div className="grid lg:grid-cols-3 gap-6">
          <div className="rounded-3xl bg-white shadow-sm border border-slate-200 p-6">
            <h2 className="text-lg font-semibold">What Claude Should Read First</h2>
            <div className="mt-4 space-y-3 text-sm text-slate-700">
              <div className="rounded-2xl bg-slate-50 border border-slate-200 p-4">
                <div className="font-medium">1. Calibration Register</div>
                <div className="mt-1 text-slate-600">No realism parameter should be edited without evidence-class awareness.</div>
              </div>
              <div className="rounded-2xl bg-slate-50 border border-slate-200 p-4">
                <div className="font-medium">2. Research Report</div>
                <div className="mt-1 text-slate-600">Use direct vs proxy vs placeholder distinctions to protect realism claims.</div>
              </div>
              <div className="rounded-2xl bg-slate-50 border border-slate-200 p-4">
                <div className="font-medium">3. Bench SOP</div>
                <div className="mt-1 text-slate-600">Backbone experiment data should replace the most exposed assumptions first.</div>
              </div>
            </div>
          </div>

          <div className="rounded-3xl bg-white shadow-sm border border-slate-200 p-6">
            <h2 className="text-lg font-semibold">M2 Expansion Gate</h2>
            <div className="mt-4 space-y-4 text-sm text-slate-700">
              {[
                "Monotonic pressure–flow behavior validated",
                "Fouling escalation behaves credibly under load",
                "Backflush recovery is partial and diminishing",
                "Pressure and flow sensing anchored to real ranges",
                "Most exposed hydraulic proxies replaced or bounded",
              ].map((item) => (
                <div key={item} className="flex items-start gap-3 rounded-2xl bg-slate-50 border border-slate-200 p-4">
                  <div className="w-2.5 h-2.5 rounded-full bg-slate-800 mt-1.5" />
                  <div>{item}</div>
                </div>
              ))}
            </div>
          </div>

          <div className="rounded-3xl bg-white shadow-sm border border-slate-200 p-6">
            <h2 className="text-lg font-semibold">City of HydrOS Hook</h2>
            <p className="mt-2 text-sm text-slate-600">
              Once the backbone is calibrated, this dashboard becomes the substrate for agent interpretation, scenario policy testing,
              maintenance advisory, and multi-expert consensus.
            </p>
            <div className="mt-4 grid gap-3">
              {[
                ["Operator Agents", "Observe flow, pressure, timing, fault posture"],
                ["Engineering Agents", "Diagnose stage burden and model mismatch"],
                ["Maintenance Agents", "Recommend recovery timing and cooldown strategy"],
                ["Policy Agents", "Compare throughput, pressure, fouling, and capture tradeoffs"],
              ].map(([k, v]) => (
                <div key={k} className="rounded-2xl border border-slate-200 p-4">
                  <div className="font-medium">{k}</div>
                  <div className="text-sm text-slate-600 mt-1">{v}</div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
```학assistant to=canmore.create_textdoc เงินไทยฟรี 񹚂 yes code block? Need valid JSON string no weird chars. redo clean.】【”】【analysis to=canmore.create_textdoc ￣色 లేదు code call failed due malformed. Need retry once. Output content clean ASCII. include no markdown fences.((&___assistant to=canmore.create_textdoc մեկնաբանություն ＿久久իկ json մեջ={
